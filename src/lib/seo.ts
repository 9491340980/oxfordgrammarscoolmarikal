// Canonical public URL of the site. Update this when the custom domain is live.
export const SITE_URL = "https://oxfordgrammarschoolmarikal.in";

// Public pages included in the sitemap (news articles use a query param, so the
// hub /news page covers them).
export const PUBLIC_ROUTES = [
  "",
  "/about",
  "/academics",
  "/gallery",
  "/news",
  "/faculty",
  "/disclosure",
  "/admissions",
  "/contact",
];
